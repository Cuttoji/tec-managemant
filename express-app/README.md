# Express App (Scaffold)

Basic Node.js + Express scaffold created by Copilot CLI runtime in VS Code.

Getting started:

1. cd express-app
2. npm install
3. npm run dev  # requires nodemon or use npm start

Endpoints:
- GET / -> health/status
- GET /health -> uptime and status

